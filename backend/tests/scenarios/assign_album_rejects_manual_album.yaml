name: assign_album rejects manual album
given:
  - photo: /manual_album_test/photo.jpg
    id_as: $photo
    asset_id_as: $asset_id
when:
  call: PUT /put/assign_album
  body:
    assetId: "${asset_id}"
    albumId: "bogus-manual-album"
    onConflict: "rename"
then:
  - response.status: 400
