---
status: idea
type: feature
priority: low
area: testing
---

Design UI DSL vocabulary for Playwright-driven E2E tests:

- Design UI DSL vocabulary (navigation, selectors, visual assertions)
- Prototype one `cargo xtask test-frontend` scenario

Deferred — backend API E2E scenarios are the priority.
